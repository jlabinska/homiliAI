spring:
  datasource:
    url: jdbc:mysql://localhost:3306/homiliAI_db
    username: root
    password: coderslab
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect